org.gradle.jvmargs=-Xmx2G -Dfile.encoding=UTF-8
org.gradle.vfs.watch=false
org.gradle.parallel=false
org.gradle.daemon=false
android.useAndroidX=true
org.gradle.java.home=C:/Program Files/Eclipse Adoptium/jdk-17.0.19.10-hotspot
# Disable experimental features that cause access violations on Windows
flutter.native-assets=false
# This builtInKotlin flag was added automatically by Flutter migrator
android.builtInKotlin=true
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false

# Override Kotlin JVM target validation to prevent build failure on inconsistent Java/Kotlin target versions
kotlin.jvm.target.validation.mode=warning

# Disable Kotlin incremental compilation to prevent daemon cache root mismatch errors on Windows
kotlin.incremental=false

